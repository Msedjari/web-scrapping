{% extends 'base.html.twig' %}

{% block title %}Equipo - Fastscore{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="section-title mb-0">
        <i class="fas fa-users"></i> Detalles del Equipo
    </h1>
    
    <a href="javascript:history.back()" class="btn btn-outline-primary">
        <i class="fas fa-arrow-left me-2"></i> Volver
    </a>
</div>

<!-- Navigation Breadcrumb -->
<nav aria-label="breadcrumb" class="mb-4">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/adminDashboard">Dashboard</a></li>
        <li class="breadcrumb-item"><a href="/adminEquipos">Equipos</a></li>
        <li class="breadcrumb-item active" aria-current="page">{{ team.club_name }}</li>
    </ol>
</nav>

<div class="row">
    <div class="col-md-5 mb-4">
        <div class="card h-100">
            <div class="card-header text-center py-3 bg-primary text-white">
                <h5 class="card-title mb-0">
                    {% if team.escudo_image %}
                    <img src="{{ team.escudo_image }}" alt="Escudo {{ team.club_name }}" class="me-2" style="max-height: 40px; vertical-align: middle;">
                    {% endif %}
                    {{ team.club_name }}
                </h5>
            </div>
            <div class="card-body">
                <div class="text-center mb-4">
                    {% if team.logo_url %}
                    <img src="{{ team.logo_url }}" alt="Logo {{ team.club_name }}" 
                         class="img-fluid" style="max-height: 150px;">
                    {% endif %}
                </div>
                
                <ul class="list-group list-group-flush">
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <span><i class="fas fa-id-badge text-primary me-2"></i> ID del Equipo</span>
                        <span class="badge bg-primary rounded-pill">{{ team.id }}</span>
                    </li>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <span><i class="fas fa-calendar text-primary me-2"></i> Fundado en</span>
                        <span>{{ team.foundation_year }}</span>
                    </li>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <span><i class="fas fa-trophy text-primary me-2"></i> Liga</span>
                        <span>{{ team.league }}</span>
                    </li>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <span><i class="fas fa-user-tie text-primary me-2"></i> Presidente</span>
                        <span>{{ team.president }}</span>
                    </li>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <span><i class="fas fa-user text-primary me-2"></i> Entrenador</span>
                        <span>{{ team.coach }}</span>
                    </li>
                </ul>
            </div>
            <div class="card-footer text-center d-flex gap-2 justify-content-center">
                <a href="{{ team.website }}" class="btn btn-primary" target="_blank">
                    <i class="fas fa-external-link-alt me-1"></i> Visitar Web
                </a>
                <a href="/adminEquiposId/edit/{{ team.id }}" class="btn btn-warning">
                    <i class="fas fa-edit me-1"></i> Editar
                </a>
            </div>
        </div>
    </div>
    
    <div class="col-md-7 mb-4">
        <div class="card h-100">
            <div class="card-header py-3 bg-secondary text-white">
                <h5 class="card-title mb-0">
                    <i class="fas fa-info-circle me-2"></i>Información del Estadio
                </h5>
            </div>
            <div class="card-body">
                <div class="mb-4">
                    <h5 class="border-bottom pb-2 text-primary">
                        <i class="fas fa-home me-2"></i>{{ team.stadium_name }}
                    </h5>
                    
                    <div class="row mt-4">
                        <div class="col-md-6">
                            <div class="info-item mb-3">
                                <p class="text-muted mb-1">Capacidad:</p>
                                <h5><i class="fas fa-users text-primary me-2"></i>{{ team.stadium_capacity }} espectadores</h5>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="info-item mb-3">
                                <p class="text-muted mb-1">Dirección:</p>
                                <h5><i class="fas fa-map-marker-alt text-primary me-2"></i>{{ team.stadium_address }}</h5>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="mt-4">
                    <h5 class="border-bottom pb-2 text-primary">
                        <i class="fas fa-history me-2"></i>Historia del Club
                    </h5>
                    
                    <p class="mt-3">
                        El {{ team.club_name }} fue fundado en {{ team.foundation_year }} y desde entonces ha sido
                        parte importante de la {{ team.league }}. Bajo la presidencia de {{ team.president }} y
                        con {{ team.coach }} como entrenador, el equipo busca nuevos éxitos deportivos.
                    </p>
                </div>
                
                <!-- Recent Results (Placeholder) -->
                <div class="mt-4">
                    <h5 class="border-bottom pb-2 text-primary">
                        <i class="fas fa-chart-line me-2"></i>Últimos Resultados
                    </h5>
                    
                    <div class="mt-3">
                        <div class="alert alert-secondary">
                            <i class="fas fa-info-circle me-2"></i>No hay resultados recientes disponibles.
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %} 