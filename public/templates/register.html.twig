<!DOCTYPE html>
<html lang="{{ lang }}"> <!-- Establecer el idioma de la página -->

<head>
    <meta charset="UTF-8"> <!-- Establecer la codificación de caracteres -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- Configurar la vista para dispositivos móviles -->
    <title>Registro</title> <!-- Título de la página -->
    <link href="bootstrap/css/bootstrap.min.css" rel="stylesheet"> <!-- Vincular CSS de Bootstrap local -->
    <!-- Vincular CSS de Bootstrap 5 desde CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="assets/css/style.css">
</head>

<body >
    <style>
        .btn1{
            background-color: #007bff; /* Color de fondo del botón personalizado */
            border-color: #007bff; /* Color del borde del botón personalizado */
            display: block; /* Mostrar como bloque */
            margin: 0 auto; /* Centrar horizontalmente */
        }
        .btn-primary:hover{
            background-color: #0056b3; /* Color de fondo al pasar el ratón */
            border-color: #004085; /* Color del borde al pasar el ratón */
        }
        .card{
            background-color:rgb(255, 255, 255); /* Color de fondo de la tarjeta */
        }
    </style>
    <div class="container mt-5"> <!-- Contenedor principal con margen superior -->
        <div class="d-flex justify-content-between"> <!-- Botones para cambiar de idioma -->
            <a href="?lang=es" class="btn btn-primary">Español</a>
            <a href="?lang=en" class="btn btn-primary">Inglés</a>
        </div>
        <div class="row justify-content-center"> <!-- Fila centrada -->
            <div class="col-md-6"> <!-- Columna de tamaño medio -->
                {% if session.error %} <!-- Mostrar mensaje de error si existe -->
                <div class="alert alert-danger" role="alert">
                    {{ session.error }}
                </div>
                {% endif %}
                {% if session.success %} <!-- Mostrar mensaje de éxito si existe -->
                <div class="alert alert-success" role="alert">
                    {{ session.success }}
                </div>
                {% endif %}
                <div class="card"> <!-- Tarjeta para el formulario de registro -->
                    <div class="card-header text-center"> <!-- Encabezado de la tarjeta -->
                        <h4>{{ trans('Crear cuenta') }}</h4>
                    </div>
                    <div class="card-body"> <!-- Cuerpo de la tarjeta -->
                        <form action="" method="post"> <!-- Formulario de registro -->
                            <div class="form-group"> <!-- Grupo de formulario para el nombre de usuario -->
                                <label for="username">{{ trans('Username') }}</label>
                                <input type="text" id="username" name="username" class="form-control" required>
                            </div>
                            <div class="form-group mt-3"> <!-- Grupo de formulario para el correo electrónico -->
                                <label for="email">{{ trans('Correo electrónico') }}</label>
                                <input type="email" id="email" name="email" class="form-control" required>
                            </div>
                            <div class="form-group mt-3"> <!-- Grupo de formulario para la contraseña -->
                                <label for="password">{{ trans('Contraseña') }}</label>
                                <input type="password" id="password" name="password" class="form-control" required>
                            </div>
                            <div class="form-group mt-3"> <!-- Grupo de formulario para confirmar la contraseña -->
                                <label for="confirm_password">{{ trans('Confirmar contraseña') }}</label>
                                <input type="password" id="confirm_password" name="confirm_password"
                                    class="form-control" required>
                            </div>
                            <div class="form-group mt-4 text-center"> <!-- Botón de envío del formulario -->
                                <button type="submit" class="btn btn-primary btn-block">{{ trans('Registrarse') }}</button>
                            </div>
                        </form>
                        <div class="form-group mt-3 text-center"> <!-- Enlace para iniciar sesión si ya tiene cuenta -->
                            <p>{{ trans('ya_tienes_cuenta') }}</p>
                            <a href="login" class="btn btn-primary">{{ trans('Iniciar sesión') }}</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="bootstrap/js/bootstrap.bundle.min.js"></script> <!-- Script de Bootstrap -->
</body>

</html>